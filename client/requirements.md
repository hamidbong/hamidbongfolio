## Packages
framer-motion | Complex animations and page transitions
lucide-react | Icons for security/devops themes (Shield, Terminal, etc.)
react-hook-form | Form handling for contact page
zod | Schema validation
@hookform/resolvers | Zod resolver for react-hook-form
clsx | Conditional class names
tailwind-merge | Merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  mono: ["var(--font-mono)", "monospace"],
  sans: ["var(--font-sans)", "sans-serif"],
}
